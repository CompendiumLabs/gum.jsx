# gum.js

# TODO

Make a generic `Spline` component that can be used to create multi-segment smooth curves (type include Catmull-Rom, B-Spline, and Bézier). Should be able to map into native SVG path data.
